@model OcdlogisticsSolution.Web.Models.ResetPasswordViewModel
@{
    ViewBag.Title = "Reset password";
    Layout = "~/Views/Shared/_RatsLayout.cshtml";

}

<!-- Login START  -->
<div id="contact_us">
    <!-- BreadCrumbs -->
    <ul class="breadcrumb cust_crumb">
        <li><a href="#"><i class="fa fa-home"></i>Home</a></li>
        <li><a class="" href="#">Reset Password</a></li>
    </ul>
    <div class="container">
        <div class="row d-flex h-100">
            <div class="col-12 col-lg-12 col-md-12 col-xl-12 ">
                <div id="e_msg"></div>
                <div class="form-main login_main_div pt-md-5 pb-md-5 cont_text">
                    @using (Html.BeginForm("ResetPassword", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "login-form cust_cntct_input", id = "login", required = "required" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary("", new { @class = "text-danger" })
                        @Html.HiddenFor(model=>model.Code)
                    <div class=" justify-center">
                        <p class="text-center log_mint_p">Reset Password </p>

                        <div class="text-center">
                            <div class="form-group">
                                @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "Email*", id = "email", required = "required" })
                            </div>
                        </div>
                        <div class="text-center ">
                            <div class="form-group">
                                @Html.TextBoxFor(m => m.Password, new { @class = "form-control", placeholder = "Password*", id = "password", required = "required", @type = "password" })
                            </div>
                        </div>
                        <div class="text-center ">
                            <div class="form-group">
                                @Html.TextBoxFor(m => m.ConfirmPassword, new { @class = "form-control", placeholder = "confirm Password*", id = "password", required = "required", @type = "password" })
                            </div>
                        </div>
                        @*<div class="text-center">
            <div class="form-group">
                @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "  Email*", id = "email", required = "required" })
            </div>
        </div>
        <div class="text-center">
            <div class="form-group">
                @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "  Password*", id = "password", type = "password", required = "required" })
            </div>
        </div>*@

                    </div>

                        <div class="text-center pt-md-3 pb-2">
                            <button type="submit" class="btn btn-primary mint_cntct_btn login_social_btn">Reset</button>
                        </div>
                        <div class="text-center pb-2">
                            <a href="@Url.Action("Index","Home")" class="btn btn-primary login_social_btn mint_cntct_btn">Return to store</a>
                        </div>

                    }
                </div>
            </div>
        </div>
    </div>
</div> <!-- Login End -->
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
